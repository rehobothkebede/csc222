#include <string>
#include <vector>
#include <algorithm>
using namespace std;

string render_num_vector(const vector<int>&);
int num_vector_sum(const vector<int>&);
int num_vector_product(const vector<int>&);
vector<int> only_evens(const vector<int>&);
vector<int> only_odds(const vector<int>&);
vector<int> nums_between(const vector<int>&,int low,int high);
double num_vector_mean(const vector<double>&);
int num_vector_median(const vector<int>&);
/*int num_vector_mode(cosnt vector<int>&);*/
