#define DATE_H
#include <string>
using namespace std;

struct Fraction{
    int numerator, denominator;
    string s;


    Fraction();
    Fraction(int numerator, int denominator);
    Fraction(string s);
    
};

// #endif
